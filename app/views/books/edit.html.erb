<div class="edit">
        <h2><strong>Editing Book</strong></h2>

          <%= form_with model: @book,url:'/books',local:true do |f| %>
           <h4>Title</h4>
            <%= f.text_field :title %>
           <h4>本文</h4>
            <%= f.text_area :body %>
            <%= link_to "Uodate Book", edit_book_path(@book.id) %>
            
          <% end %>
</div>